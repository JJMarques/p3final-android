<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pt.ismt.projfinalp3">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_app"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_app"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".AtividadesAdminActivity"></activity>
        <activity android:name=".SaudeInsertActivity" />
        <activity android:name=".SaudeAdminActivity" />
        <activity android:name=".RefeicaoInsertActivity" />
        <activity android:name=".RefeicaoAdminActivity" />
        <activity android:name=".AtividadesActivity" />
        <activity android:name=".SaudeActivity" />
        <activity android:name=".RefeicaoActivity" />
        <activity android:name=".HomeAdminActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>